FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY main.py .

EXPOSE 8080

# Gunicorn con flags directos
CMD ["gunicorn", "main:app", "-w", "1", "-k", "sync", "--timeout", "60", "--keep-alive", "2", "-b", "0.0.0.0:8080"]